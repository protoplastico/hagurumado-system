# 本番データ移行リハーサル結果報告(TASK-31)

**現状:テンプレートのみ。実リハーサルは未実施。**

本ファイルはTASK-31の受入条件「リハーサル結果を`docs/migration_rehearsal_report.md`に記録」に対応する
テンプレートである。実際のリハーサル(devプロジェクトへのBASE全期間データ取込)には、本サンドボックスに
存在しない以下が必要なため、Sonnet側では実施できなかった:

- 本番相当のdev Supabaseプロジェクトへの実アクセス(URL・service_role key)
- BASE管理画面からエクスポートした最新・全期間の注文CSV・顧客CSV(実個人情報を含むため、
  CLAUDE.mdの方針上サンドボックスに投入すべきでもない)

人間(または実データ・実環境にアクセスできるセッション)が実際にリハーサルを実施した際、
以下の見出し構成に沿って結果を記録すること。`supabase/verification/base_import_reconciliation.sql`の
出力(集計値のみ。個別の氏名・メールアドレス等は転記しないこと)をそのまま利用できる。

---

## 実施日時

（記入:YYYY-MM-DD HH:MM〜HH:MM、実施者）

## 対象データ

- 注文CSV:（エクスポート日、対象期間、行数）
- 顧客CSV:（エクスポート日、行数)※このファイル自体には実データを書かないこと

## 取込結果(件数)

| 項目 | BASE側件数 | 取込結果件数 | 一致 |
|---|---|---|---|
| 注文件数 | | | |
| 顧客件数(email名寄せ後) | | | |
| 製作アイテム件数 | | | |
| variations件数(seed_variations_full.sql適用分) | 523 | | |

## 検証SQL実行結果

`supabase/verification/base_import_reconciliation.sql`の各セクションの結果:

- §2 emailの実質重複:（0件 / N件、N件の場合は対応内容）
- §3-1 subtotal+shipping_fee=total不一致:（0件 / N件)
- §3-2 order_items単価合計とsubtotalの差異(±1円超):（0件 / N件、原因)
- §3-3 金額0円以下の異常値:（0件 / N件)
- §5 external_ref重複:（0件 / N件)

## 「便宜的に発送済」問題の仕分け結果(作業項目2)

- §4クエリで抽出された要確認注文数:（N件）
- うち実際には未発送でqueuedへ修正した件数:（N件）
- 修正方法:（使用した管理画面操作・DB関数）

## 所要時間

| 工程 | 所要時間 |
|---|---|
| CSVインポート実行 | |
| 検証SQL実行・結果確認 | |
| 未完了注文の仕分け(人手作業) | |
| 合計 | |

## エラーと対処

（インポート中に発生したエラー・スキップされた行とその理由・対処内容を記載）

## 再実行(重複なし確認)

同じCSVで`importBaseOrders`を再実行した結果、全件が`skipped`になったことを確認:（はい/いいえ）

## 本番移行(TASK-33)に向けた申し送り事項

（リハーサルで判明した、本番移行時に注意すべき点があれば記載)
